import type { ReactNode } from "react";

/**
 * Web sidebar footer (logout / login-as). Mobile drawer owns its own footer actions.
 * Export preserved for tree parity.
 */
export function DashboardSidebarFooter(_props: {
  children?: ReactNode;
  [key: string]: unknown;
}): null {
  return null;
}
