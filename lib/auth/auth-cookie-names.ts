/**
 * Session cookie names shared by the browser cookie helper and Edge middleware.
 * Keep in sync with `api/storage/auth-cookies.ts` behaviour.
 */
export const AUTH_COOKIE_ACCESS = "converge_access_token";
export const AUTH_COOKIE_REFRESH = "converge_refresh_token";
