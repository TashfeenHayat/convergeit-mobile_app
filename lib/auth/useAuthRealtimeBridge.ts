/**
 * RN stub — web realtime auth bridge depends on browser cookies / Next navigation.
 * No-op until mobile session sync sockets are wired.
 */
export function useAuthRealtimeBridge(_enabled: boolean, _token?: string): void {
  /* no-op */
}
