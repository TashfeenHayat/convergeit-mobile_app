export {
  appTheme,
  theme,
  appColors,
  defaultAppColors,
  tokens,
  spacing,
  radii,
  typography,
  mainBackgroundGradient,
  mainBackgroundGradientStops,
  createAppTheme,
  darkenHex,
} from './theme';
export type { AppTheme, AppColors, Tokens, PaletteMode } from './theme';
export { ThemeProvider, useAppTheme, useTheme } from './ThemeProvider';
export type { ThemeProviderProps } from './ThemeProvider';
