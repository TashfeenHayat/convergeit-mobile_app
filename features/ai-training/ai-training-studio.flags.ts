/**
 * Flow builder features that need Phase C (graph-driven backend).
 * Set to true when wires, add-node, and execution log are fully wired to live bot.
 */
export const FLOW_BUILDER_EXPERIMENTAL_UI = false;
