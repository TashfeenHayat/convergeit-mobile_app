import { RoleDashboardPage } from '@/features/dashboard/screens/ParityUtilityScreens';

export default function Screen() {
  return (
    <RoleDashboardPage
      title="Supervisor dashboard"
      subtitle="Monitor queues, involvement, and team attendance."
    />
  );
}
